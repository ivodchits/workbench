// The Instance Manager rail panel (the left rail). Step 1.3 ships the project
// registry; later steps grow it into the full Group → Project → Instance tree.

export { default } from "./InstanceManager";
