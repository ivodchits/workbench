// The Instance Manager rail panel (the left rail). Step 1.4 ships the full
// Group → Project → Instance tree with instance CRUD, inline task notes, and
// row actions; live status and console wiring arrive in 2.2 and 1.5.

export { default } from "./InstanceManager";
