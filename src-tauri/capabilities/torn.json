{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "torn-window",
  "description": "Capability for torn-off panel windows (step 4.2 — OS-window tear-off). Each torn-off console/shell lives in its own webview, attaches to the live PTY as a second multiplexer subscriber (step 4.1), reads prefs for theming, and can close itself to dock the panel back.",
  "windows": ["torn-*"],
  "permissions": [
    "core:default",
    "store:default",
    "core:window:allow-close"
  ]
}
